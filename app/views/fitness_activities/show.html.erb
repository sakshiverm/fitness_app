<!DOCTYPE html>
<html>
<head>
  <title>User Activity Details</title>
  <%= stylesheet_link_tag 'fitness_activity_show' %>
</head>
<body>
  <div class="container">
    <h1>User Activity Details</h1>

    <p><strong>steps_taken:</strong> <%= @fitness_activity.steps_taken %></p>
    <p><strong>calories_burned:</strong> <%= @fitness_activity.calories_burned %></p>
    <p><strong>workout_duration:</strong> <%= @fitness_activity.workout_duration %> days</p>

    <div>
      <%= link_to 'Edit', edit_fitness_activity_path(@fitness_activity), class: "btn btn-primary" %>
      <%= link_to 'Back', fitness_activities_path, class: "btn btn-secondary" %>
      <%= link_to 'Destroy', fitness_activity_path(@fitness_activity), method: :delete, data: { confirm: 'Are you sure you want to delete this fitness activity?' }, class: "btn btn-primary" %>
    </div>
  </div>
</body>
</html>
