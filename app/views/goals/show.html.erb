<!DOCTYPE html>
<html>
<head>
  <title>Goal Details</title>
  <%= stylesheet_link_tag 'goal_index' %>
</head>
<body>
  <div class="container">
    <h1>Goal Details</h1>

    <div class="card mb-3">
      <div class="card-body">
        <p><strong>Name:</strong> <%= @goal.name %></p>
        <p><strong>Description:</strong> <%= @goal.description %></p>
        <p><strong>Duration:</strong> <%= @goal.duration %> days</p>
      </div>
    </div>

    <div class="d-flex justify-content-between">
      <%= link_to 'Edit', edit_goal_path(@goal), class: "btn btn-primary" %>
      <%= link_to 'Back', goals_path, class: "btn btn-secondary" %>
      <%= link_to 'Destroy', goal_path(@goal), method: :delete, data: { confirm: 'Are you sure you want to delete this goal?' }, class: "btn btn-primary" %>
    </div>
  </div>
</body>
</html>
