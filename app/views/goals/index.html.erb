<!DOCTYPE html>
<html>
<head>
  <title>Goals</title>
  <%= stylesheet_link_tag 'goal_index' %>
</head>
<body>
  <div class="container">
    <h1>Goals</h1>

    <ul>
      <% @goals.each do |goal| %>
        <li><%= link_to goal.name, goal %></li><br>
      <% end %>
    </ul>

    <%= link_to 'New Goal', new_goal_path, class: "btn btn-primary" %>
  </div>
</body>
</html>


